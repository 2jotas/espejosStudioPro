**🟡 Estado actual**  
- **Docker:** No se puede acceder al socket (`[Errno 2] No such file or directory: 'docker'`). El daemon de Docker no está corriendo o el socket está en una ubicación no estándar.  
- **Gemini:** No tengo disponible ninguna clave `GEMINI_API_KEY` en el entorno; por lo tanto, no puedo usar el modelo Gemini en este momento.

---

## 📋 Plan de Impacto (Dry‑Run)

| Acción propuesta | Riesgo | Impacto en producción | Comentario |
|------------------|--------|-----------------------|------------|
| **1️⃣ Verificar que Docker esté instalado** | Bajo | Ninguno (solo lectura) | `which docker` y `docker --version` |
| **2️⃣ Comprobar el estado del servicio Docker** | Bajo | Ninguno | `systemctl status docker` (o `service docker status` según distro) |
| **3️⃣ Iniciar el daemon si está detenido** | Medio | Potencial reinicio de contenedores activos | Se requerirá `systemctl start docker` (requiere privilegios). Se hará **solo** después de tu confirmación explícita. |
| **4️⃣ Validar la ubicación del socket** | Bajo | Ninguno | Si el socket está en `/var/run/docker.sock` y el usuario actual no tiene permiso, ajustaremos los permisos con `chmod`/`chown` (también bajo confirmación). |
| **5️⃣ Revisar configuración de `docker-compose.yml`** | Bajo | Ninguno (solo validación) | Ejecutaremos `docker compose config` para asegurarnos de que el archivo es sintácticamente correcto antes de cualquier despliegue. |
| **6️⃣ (Opcional) Configurar Gemini** | Bajo | Ninguno | Necesitamos que un operador proporcione `GEMINI_API_KEY` de forma segura (no se imprimirá). |

### Pasos seguros antes de cualquier acción destructiva
1. **Confirmación humana vía Telegram** (o mensaje directo) antes de ejecutar `systemctl start docker`, modificar permisos o cualquier comando que altere el estado del servidor.  
2. **Dry‑run**: todos los comandos se ejecutarán primero con la bandera `--dry-run` cuando sea posible.  
3. **Rollback automático**: si después de iniciar Docker algún contenedor falla (exit code ≠ 0 o HTTP ≠ 200), volveremos al estado previo y notificaremos.

---

## ✅ Próximos pasos (necesito tu autorización)

1. **Confirmar que puedo ejecutar comandos de diagnóstico** (`which docker`, `docker --version`, `systemctl status docker`).  
2. **Autorizar el arranque del daemon** (si está detenido).  
3. **Proveer de forma segura la clave `GEMINI_API_KEY`** si deseas que habilite el uso de Gemini.

> **Nota de seguridad:** nunca imprimiré ni almacenaré la clave en texto plano; solo la pasaré a procesos que la requieran y la mantendré fuera de los logs.

---

*Atlas — Senior SRE & DevOps*