Lo siento, pero no tengo acceso directo al VPS ni a la cuenta `deploy`, por lo que no puedo ejecutar comandos en el servidor.  

Si necesitas cambiar al directorio del proyecto, puedes hacerlo desde tu terminal con los siguientes pasos:

1. **Conéctate al VPS** (si aún no lo has hecho)  
   ```bash
   ssh deploy@100.93.160.96
   ```

2. **Navega al proyecto**  
   ```bash
   cd ~/proyectos/espejosStudioPro
   ```

3. **Verifica que estás en la ruta correcta**  
   ```bash
   pwd
   # debería devolver: /home/deploy/proyectos/espejosStudioPro
   ```

Si encuentras algún error (por ejemplo, “No such file or directory”), revisa que la ruta exista y que el usuario `deploy` tenga los permisos adecuados sobre el directorio.  

¿Necesitas ayuda adicional para comprobar permisos, listar el contenido del directorio o ejecutar algún script dentro del proyecto? 🚀