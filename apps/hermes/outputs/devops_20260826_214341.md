**Próximos pasos – flujo de trabajo estándar**  

1️⃣ **Auditoría de Pull Requests**  
   - Revisar los cambios pendientes en los repositorios **`2jotas/espejosStudioPro`** y **`2jotas/formacion-ciencia-datos`**.  
   - Verificar:  
     - Cumplimiento de normas de ciberseguridad (exposición de credenciales, uso de paquetes vulnerables).  
     - Pasada de linters (ESLint, Prettier, RuboCop, etc.).  
     - Cobertura y resultados de pruebas unitarias (`npm test`, `pytest`, etc.).  
     - Que los archivos **`.env`** estén marcados como *protected* y no se incluyan en el commit.  

2️⃣ **Validación automática (CI)**  
   - Ejecutar los pipelines de GitHub Actions / GitLab CI para cada PR.  
   - Confirmar que todos los jobs finalicen con **🟢**.  

3️⃣ **Chequeo de salud de contenedores (VPS – Tailscale 100.93.160.96)**  
   - Conectarse al nodo vía `tailscale ssh` y correr:  
     ```bash
     docker ps --format "table {{.Names}}\t{{.Status}}"
     docker inspect --format='{{json .State.Health}}' <container_name>
     ```  
   - Estado esperado:  
     - **Gateway Nginx** (puertos 8080/8443) → 🟢  
     - **API Fastify** → 🟢  
     - **Web React** → 🟢  
     - **Postgres** → 🟢  
     - **Redis** → 🟢  

4️⃣ **Monitoreo de logs críticos**  
   - `docker logs -f <container>` para cada servicio y buscar errores (`ERROR`, `WARN`).  
   - Si aparecen advertencias, anotarlas y decidir si requieren mitigación antes del despliegue.  

5️⃣ **Aprobación de despliegue**  
   - Generar el mensaje de solicitud de aprobación de 1‑clic en Telegram (bot configurado).  
   - Mensaje típico:  
     ```
     🚀 *Despliegue a producción*  
     Repositorio: espejosStudioPro  
     PR #: 42  
     Cambios: <breve descripción>  
     ✅ Aprobar | ❌ Rechazar
     ```  
   - Esperar confirmación del responsable de producto/seguridad.  

6️⃣ **Despliegue en producción** (solo tras aprobación)  
   - `docker compose -f docker-compose.prod.yml up -d --pull always`  
   - Verificar nuevamente la salud de los contenedores (paso 3).  

7️⃣ **Post‑despliegue**  
   - Ejecutar pruebas de humo (`curl https://api.espejosstudio.pro/health`).  
   - Actualizar el tablero de incidentes y notificar al equipo.  

---

### Resumen rápido (emoji)

| Paso | Acción | Estado esperado |
|------|--------|-----------------|
| 1️⃣ | Revisar PRs | 🟢 |
| 2️⃣ | CI pasa | 🟢 |
| 3️⃣ | Contenedores saludables | 🟢 |
| 4️⃣ | Logs limpios | 🟢 |
| 5️⃣ | Aprobar en Telegram | ✅ |
| 6️⃣ | Deploy | 🟢 |
| 7️⃣ | Smoke test + notificación | 🟢 |

**¿Todo claro?** Si necesitas que inicie alguna de estas tareas ahora (por ejemplo, revisar PRs específicas o lanzar el chequeo de salud), indícame el objetivo y lo ejecuto.